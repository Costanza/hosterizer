meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{base_url}}/health
  body: none
  auth: none
}

docs {
  # Health Check
  
  Verify that the auth service is running and healthy.
  
  ## Expected Response
  - Status: 200 OK
  - Body: "OK"
}

tests {
  test("should return 200 OK", function() {
    expect(res.status).to.equal(200);
  });
  
  test("should return OK message", function() {
    expect(res.body).to.equal("OK");
  });
}
